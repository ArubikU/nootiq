#create a referrals system
to the register page add a optional togable field to insert a referral code.


i want also upgrade the promo-codes system
with "types" so we can have different promo codes, separated, by type.
so people cant claim more than one promo code of the same type.

example when a user tries to claim a "new_user" promo code and they already have one applied, they should receive a message indicating they can't use multiple "new_user" codes.

or example a referral code can also be used as a promo code, but it will have its own type and restrictions.
called "referral"
for now a user can create 1 referral code.
this code allow giving premium plan with 1 day of duration.

the user should be allowed to see that info on their custom-user dialog menu.


#create the 001.sql script so i can update the db